# Angel Live APK

This package is configured for GitHub Actions.

1. Replace the repository contents with this package.
2. Push to `main`.
3. Open GitHub Actions.
4. Run `Build Angel Live Android APK`.
5. Download the `Angel-Live-APK` artifact.

Capacitor uses `capacitor.config.json`; stale TypeScript/JavaScript Capacitor configs are removed by the workflow.
